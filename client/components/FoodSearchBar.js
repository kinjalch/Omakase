import React from 'react';

const FoodSearchBar = (props) => (
    <div>
        <h1> What are you craving? </h1>
        <input placeholder="Enter a Food Item"/>
        <button> Curate The Best </button>
    </div>
);

export default FoodSearchBar;
